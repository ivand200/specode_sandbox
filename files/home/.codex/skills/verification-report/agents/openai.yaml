interface:
  display_name: "Verification Report"
  short_description: "Summarize proof and residual risk."
  default_prompt: "Use $verification-report after implementation, testing, required review, and accepted reviewer fixes to summarize the key proof, tests to inspect, manual checks, and residual risks before done."
